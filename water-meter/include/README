This directory is intended for project header files.

A header file with the same name as the source file allows you to
include project specific (private) header files from the src/
folder via #include "file.h".
